# Appwrite (client-safe values only — never put an API secret here)
VITE_APPWRITE_ENDPOINT=https://nyc.cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=

# API keys belong only in Appwrite Sites / Functions env vars, never in git.
