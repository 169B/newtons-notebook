# Appwrite (client-safe values only — never put an API secret here)
VITE_APPWRITE_ENDPOINT=https://nyc.cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=
VITE_APPWRITE_DATABASE_ID=newton_analytics
VITE_APPWRITE_COLLECTION_VISITORS=visitors

# One-time setup only (local shell — never commit real values):
# APPWRITE_API_KEY=
# npm run setup:analytics
